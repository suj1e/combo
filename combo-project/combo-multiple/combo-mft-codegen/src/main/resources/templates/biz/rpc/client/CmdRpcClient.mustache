package {{pkg}};

import {{group}}.api.ApiConstant;
import {{group}}.api.{{pkgPrefix}}.{{uPrefix}}CmdRpcApi;
import org.springframework.cloud.openfeign.FeignClient;

/**
 * @author sujie
 * @since 1.0.0
 */
@FeignClient(contextId = "{{prefix}}CmdRpcClient", name = ApiConstant.SERVER_NAME, path = ApiConstant.SERVER_PATH + {{uPrefix}}CmdRpcApi.PATH)
public interface {{cls}} extends {{uPrefix}}CmdRpcApi {


}
